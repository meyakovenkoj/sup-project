{
    "title" : "Project",
    "required": [
        "project_id",
        "title",
        "created",
        "participants",
        "tasks",
        "status"
    ],
    "properties": {
        "project_id": {"bsonType": "ObjectId"},
        "title": {"bsonType": "string"},
        "head": {"bsonType": "object"},
        "created": {"bsonType": "date"},
        "participants": {"bsonType": "array"},
        "tasks": {"bsonType": "array"},
        "status": {"enum": ["open", "closed", "archived"]}
    }
}